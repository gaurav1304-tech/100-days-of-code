/*Write a program to find and display the sum of the first n natural numbers.*/
#include <stdio.h>
int main()
{
int n, sum;
printf("The number of terms are: ");
scanf("%d", &n);
sum = (n/2)*(n+1);
printf("The sum of first n natural number is: %d\n", sum);
return 0;
}


